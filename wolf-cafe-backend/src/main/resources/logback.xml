<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- CloudWatch Appender -->
    <appender name="CLOUDWATCH" class="wolfcafe.logs.CloudWatchAppender">
        <!-- REQUIRED CONFIG -->
        <logGroupName>SpringBootLogs</logGroupName>
        <logStreamName>LocalStream</logStreamName>
        <region>us-east-2</region> <!-- CHANGE to your region -->


        <useDefaultCredentialsProvider>true</useDefaultCredentialsProvider>


        <!-- Optional: custom message format -->
        <layout class="ch.qos.logback.classic.PatternLayout">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </layout>
    </appender>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger -->
    <root level="info">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="CLOUDWATCH" />
    </root>

</configuration>
